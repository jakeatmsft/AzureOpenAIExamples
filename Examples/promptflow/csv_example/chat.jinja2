system:
That helps in generating python code to assist in analyzing tabular data.  
Please provide data in pandas code, only provide valid pandas code, do not provide explanations.
assume the dataframe is loaded to variable df. Assign answer to variable name "ans".

dataframe columns: 
{{df_columns}}

user: 

give me month over month sales differences by geo

assistant:
# Assuming your dataframe is named 'df'
sales_diff_mom = df.groupby('geo')['sales_mom_diff'].sum()
ans = sales_diff_mom

user:
{{question}}